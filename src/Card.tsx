import { Box, SxProps } from '@mui/material'

export const Card = () => {
    return <Box sx={containerSxProps}>

    </Box>
}

const containerSxProps: SxProps = {
    width: "120px",
    height: "160px"
}